# Invariant map

This map connects GraphTruth's current design invariants to repository evidence
and the next missing proof. It is a non-normative status document, not a protocol
specification or a claim that the personal system is complete. Zone 3 evidence
shows only what the named experiment exercised.

Status terms are deliberately narrow:

- **Documented** — the invariant is stated, but has no portable executable proof.
- **Partly exercised** — a bounded implementation or fixture tests part of it.
- **Exercised for S0-S1** — the recorded runtime-boundary rehearsal tested the
  named behavior on its declared Darwin setup.

## Current map

| Invariant | Current mechanism and evidence | Current gap or next proof |
| --- | --- | --- |
| Epistemic authority stays explicit: assertion history, assessment, and acceptance are separate. | **Partly exercised.** [Principles](PRINCIPLES.md) and [RFC 0000](../rfcs/0000-project-foundation.md) define the separation. The S0-S1 worker retains provisional candidates without issuing acceptance decisions. The [Issue #42 golden journey](../examples/experiments/record-and-bundle-golden-journey-v1/RUN-REPORT.md) separately records an assertion revision, a non-decisive assessment, policy-scoped acceptances, and an explicit revocation; both readers reject invalid decisions. The [Issue #45 dogfood](../examples/experiments/murmurmark-echo-lab-correction-v1/DISPOSITION.md) correctly contains no acceptance decision; its richer assessment shape was explicitly not admitted by `shrink`. | Both envelopes remain Zone 3. No normative or general schema, real policy-scoped acceptance workflow, branching revision, optional-extension behavior, or repeated end-to-end review exists. |
| Exact evidence and provenance survive extraction. | **Exercised for S0-S1; partly exercised by two portable readers, one real/public episode, and a qualified workbench candidate.** The [runtime-boundary rehearsal](../runtime/rehearsal/observed.md) retained exact source bytes, content digests, and byte-and-line anchors in a verified hash chain. Issue #42 verifies three declared source files and exact evidence in Node and detached Python. Issue #45 closes eight exact public source snapshots and links every material positive claim to hashed byte and line spans. The [Issue #48 stop](../examples/experiments/incremental-capture-second-public-correction-v1/DISPOSITION.md) preserved its adverse result when the next interface failed before extraction. The selected [owner-facing workbench candidate](../runtime/experimental/owner-facing-workbench-v1/STATUS.md) synthetically qualifies one-file-at-a-time capture, exact spans, hashes, byte bounds, source snapshots, source drift, and unsafe-path refusal. | Locator and provenance semantics remain experimental. The candidate has no owner walkthrough evidence yet. Transformations, pagination and access gaps, semantic grounding, redaction, unavailable evidence, binary sources, migration, and successful extraction from a second real episode are not specified or proven. |
| History is revised by addition and remains reconstructible at earlier horizons. | **Partly exercised.** The runtime exposes current and recorded-as-of dossiers from an anchored append-only chain and rejects canonical tampering. Issue #42 preserves stable H1–H3 views over an append-only revision, includes a retroactive valid-time correction at H3, and makes two policies diverge at H2 without rewriting earlier records. Issue #45 keeps H1 independently reconstructible after H2 and records the correction as an additive assertion revision. The selected workbench candidate qualifies sequential named horizons, one atomic close, automatic predecessor links, exact journal-prefix retention, and H1 as-of isolation before the real GraphTruth [H1/H2 owner episode](../runtime/experimental/owner-facing-workbench-v1/EPISODE.md). | The workbench is untried by its owner. General revision, withdrawal, branching, identity merge/split, uncertain or coarse time, migration, and repeated real-work reconstruction remain unimplemented. |
| Canonical meaning does not live only in a projection. | **Exercised for S0-S1; partly exercised by three Zone 3 implementations.** Projection deletion and clean rebuild preserved the S0-S1 digest. Issue #42 exactly rebuilds a closed 19-file portable bundle in Node and detached Python. Issue #45 deletes and byte-exactly rebuilds all three supported projections from its verified 27-record, 40-file bundle. The workbench candidate synthetically qualifies byte-exact supported rebuild, projection freshness states, and refusal to repair canonical damage. | All three formats remain experiments. Issue #48 stopped before producing a real bundle or projection, and the workbench has no owner walkthrough. Cross-version migration, backup/restore, static human fallback, broader independent implementations, and normative semantics remain open. |
| Explicit unknowns survive rather than becoming unsupported answers. | **Partly exercised.** Issue #45 records the absent incident report as a first-class open question tied to the declared eight-file source closure, and its dossier abstains from causal, execution, production-safety, and complete-resource-bound claims. The owner retained this question behavior in the `shrink` subset. The workbench candidate synthetically qualifies `partial` coverage, forbids automatic absence inference, and retains unresolved questions in current and as-of views. | The workbench has no owner evidence. One targeted episode does not establish question closure, prioritization, acquisition, resolution, or behavior over inaccessible source boundaries. |
| Generated documents and other derived views expose their support, horizon, and freshness and cannot corroborate themselves. | **Partly exercised.** [Principles](PRINCIPLES.md), [Vision](VISION.md), and the [idea map](IDEAS.md#guarded-project-documentation-from-code-and-work-evidence) require attributable views over canonical evidence. The Issue #45 dossier exposes exact support and separate H1/H2 horizons and rebuilds from the bundle. | No dependency invalidation, sentence-level support checker, common-ancestry guard, circularity fixture, freshness transition, or generated-project-document comparison exists. |
| Domain organization remains purpose-relative, overlapping, and historically attributable. | **Documented.** [Vision](VISION.md#domain-organization-evolves-with-the-corpus) and the [idea map](IDEAS.md#emergent-purpose-relative-organization) preserve unclassified material, multiple memberships, and versioned topology generations. | No representative corpus has compared ordinary facets with richer organization. [Issue #8](https://github.com/asukhodko/graphtruth/issues/8) remains an open, deferred parent hypothesis; it is eligible only after the full three-corpus horizon reaches `keep`, and any executable experiment needs a separate issue and authorization. |
| Future material, oracle data, credentials, network, and outside paths stay outside the worker boundary. | **Exercised for S0-S1.** The recorded Darwin Seatbelt run passed the declared boundary probe for two independent synthetic packs. | The mechanism is Darwin-only, assumes trusted stable code and input roots, excludes malicious same-UID races, and has not admitted the changed evaluated GraphTruth/SUT runtime. |
| Retry, interruption, and rebuild do not silently duplicate or rewrite meaning. | **Exercised for S0-S1.** The report covers exact redelivery, both declared process-crash points, clean rerun equivalence, and projection rebuild. | Operating-system and power-loss durability, concurrent readers, an atomic projection-generation switch, and longer replay histories are untested. |
| Controlled temporary state and child processes can be deleted without deleting retained evidence. | **Exercised for S0-S1.** Whole-run deletion, temporary-bundle cleanup, and child-process joining passed; the [owner confirmation](../runtime/rehearsal/owner-signoff.json) is bound to the exact report. | The confirmation excludes snapshots, backups, swap, physical-media erasure, uncontrolled copies, and malicious concurrent processes; no private-data deletion drill exists. |
| Disclosure, retention, and deletion boundaries cover every declared retained and derived copy. | **Documented.** [Principles](PRINCIPLES.md#17-prefer-local-control-and-explicit-disclosure), the [development process](DEVELOPMENT.md#private-dogfood-boundary), and experiment gates require explicit processors, purposes, storage, retention, and deletion limits. | No product-level threat model, label propagation, disclosure-closure inventory, backup/index/model-copy purge drill, or portable privacy semantics exists. Experiment-specific controls do not establish this broader invariant. |
| Experiment claims remain inside frozen information, scoring, and resource boundaries. | **Partly exercised.** The synthetic preflight, G1 twin, and runtime-boundary rehearsal are mutation-tested public evidence. Issue #24 preserved accepted selection, source, and projection identities while terminally isolating failed evaluation and qualification identities; its bounded [safe result](../examples/experiments/author-call-qualification-v1/exploratory-learning-v2/SAFE-RESULT.md) remains procedure evidence only. Issue #42 froze its contract before implementation, retained both development failures, and passed 18/18 cells plus all 16 mutation classes. Issue #45 froze source, denominator, baseline, budgets, severe errors, and decision rules before adaptation, then retained its adverse baseline cell and recorded owner `shrink` separately. Issue #48 separated the four decision layers, retained all eight cells after a terminal acquisition failure, performed no retry or rescore, and recorded owner `stop` separately. [Issue #50](https://github.com/asukhodko/graphtruth/issues/50) records an owner stop before any semantic or selection freeze; it correctly claims no technical evidence. Issue #52 froze workbench scope, limits, severe errors, utility budgets, and decision rules before implementation, then bound one exact candidate after 35/35 synthetic tests. | Issue #24 produced no evaluated GraphTruth result. Issue #42 remains synthetic. Issue #45 exposed a decision-contract defect; Issue #48 repaired that contract but exposed an implementation-qualification gap in mixed-case inventory ordering. Issue #50 did not address that gap. The workbench candidate now closes that synthetic comparator gap, including permutations and the exact M10 regression, but has no owner walkthrough or utility decision. A fresh comparison is justified only by a named product decision. None of these experiments or contracts establishes generality, compatibility, or normative behavior. |
| Unknown required semantics fail visibly and optional material survives compatible evolution. | **Partly exercised.** [Architecture](ARCHITECTURE.md), [principles](PRINCIPLES.md), and RFC 0000 require preserve-or-refuse behavior and explicit versions. Both Issue #42 readers reject its unknown required record-format mutation with the same deterministic code. | No normative [specification](../spec/README.md), maintained [schemas](../schemas/README.md), optional-extension preservation fixtures, migrations, or independent conformance implementation exists. The detached reader is independent of the Node runtime, not of the experiment design. |

## Maintenance rule

Update a row only with linkable evidence. A passing Zone 3 test may narrow a gap,
but it does not make runtime behavior normative. Promotion into portable protocol
behavior requires the applicable RFC or specification text, machine-checkable
fixtures where possible, and independent conformance evidence where the roadmap
requires it.
