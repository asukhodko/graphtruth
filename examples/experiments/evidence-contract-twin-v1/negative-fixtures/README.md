# Negative fixtures

The sandbox policy lists boundary cases that M2 must execute against the exact
changed runtime. Only a harmless controller-only future canary is stored here.
Unsafe, platform-specific, or binary cases are generated in an isolated test
directory and never against the repository checkout.

Their declaration is an obligation, not evidence that a rehearsal passed.
