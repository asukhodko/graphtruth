# M10 baseline result frozen

Baseline result identity:
`incremental-capture-second-public-correction-v1-baseline-result-v1`.

- result manifest SHA-256:
  `85b9f159b7b011f42551eeca05a920a206bf35be4a5f1b2c86a9566902fa064b`;
- attempts:
  1/1;
- fixed queries:
  6/12 allowed, all completed;
- search time:
  63.654 ms;
- answer time:
  57.223 s;
- H1 answer:
  retained before H2 search;
- cell statuses:
  answered, answered, abstained, answered;
- retries, resume, or result replacement:
  none;
- scoring:
  pending the single GraphTruth run.

The baseline files are frozen. The next action starts the one allowed
GraphTruth run against the already frozen implementation.
